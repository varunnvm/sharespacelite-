"use client";

export function Toaster(_props: { richColors?: boolean }) {
  // sonner is not installed yet in this scaffold.
  // Keep this component as a safe no-op until the notification UI is added.
  return null;
}



